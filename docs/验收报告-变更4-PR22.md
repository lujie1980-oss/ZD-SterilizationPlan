# 振德医疗 · 灭菌排产 · 变更-4 · 正式验收报告（PR #22）

| 项 | 内容 |
|----|------|
| 验收对象 | [PR #22](https://github.com/lujie1980-oss/ZD-SterilizationPlan/pull/22) · 分支 `cursor/vue3-compact-ui-c8c0` |
| 被测提交 | `b9c39924abbbf7858ac0e91193f8535056ee9b9a`（`feat: migrate SPA to Vue 3 compact UI (change-4)`） |
| 相对 GitHub `main`（`7799527`） | `7799527`…`b9c3992` · **+3901 / −2658**（45 files） |
| 计划依据 | 《变更-4 测试提纲》v0.1；《详细设计》v1.0（整仓 Vue 3 + 中度紧凑；行为零回退 B1–B9） |
| 执行日 | 2026-09-17 |
| 执行方式 | `npm install` → `npm test` → `npm run build` + 源码静态核对（入口/路由/令牌/Docker 文件）+ domain 单测映射 B1–B9 + jsdom 路由/组柜/甘特抽查。**本 VM 无 Docker daemon、无 MateBook 浏览器对照截图** |
| 验收角色 | 测试侧验收执行者（**不改业务功能代码与测试断言**；本文件为唯一新增交付） |

---

## 1. 总结论

**带风险通过。** 变更-4 在 `b9c3992` 上主应用可指认为 Vue 3.5 + Router + Pinia；关键页 `/pool` `/pack` `/gantt` `/validation` 均为 `.vue`；旧 `src/ui` 目录已删除、不形成可运行入口。中度紧凑令牌与设计 §7 / 示意（侧栏 168、行高 30、顶栏 40、控件 28）对齐且已接到 CSS。B1–B9 在 domain 单测 + 接线抽查上无回退。`npm test` **183 / 183 通过**，`npm run build` 退出码 0。

未阻塞项（按本轮 C4 执行口径记为风险，不升格失败）：

- **视觉截图待 MateBook 补**（同分辨率前后对照、首屏行数量测）。
- **B1–B9 UI 路径由 MateBook 短冒烟补**（本轮为静态/单测/jsdom）。
- **Docker 实跑在 MateBook**（本 VM 无 `docker`；compose / nginx / Dockerfile 静态审阅通过）。

**无必改项。** 柜8 演示冲突属故意数据，**不当失败**。

| 用例ID | 结果 |
|--------|------|
| C4-01 Vue3 可指认 | **通过** |
| C4-02 中度紧凑令牌 | **通过**（令牌+CSS 对照设计 §7；**视觉截图待 MateBook 补**） |
| C4-03 B1–B9 零回退 | **通过**（**静态/单测覆盖，UI 路径由 MateBook 短冒烟补**） |
| C4-04 `npm test` / `npm run build` | **通过**（183 / 183；build 退出码 0） |
| C4-05 Docker 18080→80 + SPA fallback | **通过（静态）**（**Docker 实跑在 MateBook**） |

---

## 2. 环境与命令结果

| 项 | 结果 |
|----|------|
| 工作目录 | `/workspace` |
| HEAD（被测） | `b9c39924abbbf7858ac0e91193f8535056ee9b9a` |
| Node | v22.14.0 |
| npm | 10.9.7 |
| OS | Linux 6.12.94+ |
| 已装 Vue 栈 | `vue@3.5.42` · `vue-router@4.6.4` · `pinia@3.0.4` · `@vitejs/plugin-vue@5.2.4` |
| 命令顺序 | `npm install` → `npm test` → `npm run build` |
| GitHub Checks | PR #22 当时 **no checks reported**（不作为本轮功能失败） |
| Docker | **本 VM `docker: command not found`**，未执行 `docker compose up` |

### 2.1 `npm install`

- **退出码**：0
- added 159 packages，audited 160 packages（约 2s）
- 弃用警告：`whatwg-encoding@3.1.1`（jsdom 传递依赖）
- `abbrev@5` / `nopt@10` EBADENGINE（要求更高 Node patch，当前 v22.14.0）——传递依赖，**不作为功能验收失败**
- npm audit：2 moderate；**不作为功能验收失败**

### 2.2 `npm test`（`vitest run` v3.2.7）

- **退出码**：0
- **Test Files 17 passed / Tests 183 passed (183)**，Duration ~3.22s

| 文件 | 用例数 | 本轮相关性 |
|------|--------|------------|
| `src/app/__tests__/change4-vue.test.ts` | 7 | C4-01 / C4-02 令牌 / C4-05 文件约定 / jsdom 四路由 |
| `src/domain/__tests__/change1-grouping.test.ts` | 31 | B1 / B2 / B3 / B6 / B7 |
| `src/domain/__tests__/change1-boot.test.ts` | 1 | B3 jsdom：点行不勾选 |
| `src/domain/__tests__/change1-ui.test.ts` | 5 | 组柜工具条无 date/shift；热修码文案 |
| `src/domain/__tests__/pack-suggest-policy.test.ts` | 17 | B4 / B1 / B7 |
| `src/domain/__tests__/schedule-sort-policy.test.ts` | 15 | B5 / B2 |
| `src/domain/__tests__/change2-ui.test.ts` | 2 | B5 jsdom：保存不同步 |
| `src/domain/__tests__/commit-gate.test.ts` | 15 | B6 auto 拒硬错 / manual 可落盘 |
| `src/domain/__tests__/rule-engine.test.ts` | 21 | B7 `BOX_LIMIT` 口径2 |
| `src/domain/__tests__/excel-r1-r5.test.ts` | 14 | B7 大箱≤280 不误报 |
| `src/domain/__tests__/plan-store.test.ts` | 16 | B8 键名 / 策略与 `scheduleMode` hydrate |
| `src/domain/__tests__/facts.test.ts` | 9 | 主线第2节待排事实 |
| `src/domain/__tests__/entry-scheduler.test.ts` | 9 | 甘特进炉接线未回退 |
| 其余（min-load / split / suggest / export） | 21 | 变更-1/迭代 A 未回退 |

相对变更-2 验收时的 176 例，本提交新增 `change4-vue.test.ts` 7 例 → **183**，与开发 PR 正文自报一致。

### 2.3 `npm run build`（`vue-tsc --noEmit && vite build`）

- **退出码**：0
- Vite v6.4.3，97 modules transformed，built in 1.11s
- 产物：`dist/index.html` 0.67 kB gzip 0.49 kB；`dist/assets/index-mtqjUkI1.css` 24.09 kB gzip 5.36 kB；`dist/assets/index-Cs1AfS20.js` 193.99 kB gzip 68.24 kB；关键页按路由拆出 `PackCabinetView` / `GanttPlanView` / `DemandPoolView` / `ValidationCenterView` chunk
- `vue-tsc --noEmit` 无类型错误（build 脚本前半段）

---

## 3. C4-01 Vue 3 与关键页可指认

**结论：通过。**

| 步骤 | 操作 | 实际 |
|------|------|------|
| 1 | `package.json` / 锁文件 | `"vue": "^3.5.22"`（已装 **3.5.42**）；`vue-router`、`pinia`、`@vitejs/plugin-vue`、`vue-tsc` 均在。无 React。`scripts.build` = `vue-tsc --noEmit && vite build` |
| 2 | `src/main.ts` / `src/App.vue` | `main.ts` 调 `createSterilizationApp()` 后 `app.mount('#app')`。`create-app.ts` 使用 `createApp(AppRoot)` + `createPinia()` + `createWebHistory()`。`App.vue` 为 `<script setup>`，hydrate 后渲染 `AppShell` |
| 3 | `src/app/router/index.ts` | `/pool` → `DemandPoolView.vue`；`/pack` → `PackCabinetView.vue`；`/gantt` → `GanttPlanView.vue`；`/validation` → `ValidationCenterView.vue`。`/` 重定向 `/pack` |
| 4 | `src/app/views/` | 上述四文件存在且非空；另有工作台/主数据页，不挡关键页指认 |
| 5 | 旧入口与边界 | **`src/ui` 目录不存在**（PR 删除 `src/ui/app.ts`、`src/ui/dom.ts`）。`index.html` 仅 `/src/main.ts`。仓库内无 `from 'react'`。全仓 `.vue` **无** `localStorage` 字面量；读写仅 `src/persistence/plan-store-v2.ts`。`planStore` 经 `loadPlan`/`savePlan`，不直接 `localStorage.getItem/setItem` |
| 6 | 四路由可打开 | jsdom：`change4-vue.test.ts` 挂载后 `/pack` `#page-grouping`、`/pool` `#page-pool`、`/gantt` `#page-furnace-plan`、`/validation` `#page-validation` 均可渲染。真实 18080 浏览器路径 **UI 路径由 MateBook 短冒烟补** |

### 3.1 `.vue` 不复制 domain 约束

抽查：

- `PackCabinetView.vue` 调用 `listEligibleForCabinet` / `listCandidateCabinets` / `validateFurnace`；`GROUPING_ISSUE_CODES` 仅为展示过滤集合，不实现托盘/箱限算法。
- `planStore.ts` 组柜走 `autoPackCabinet` / `manualPackCabinet` / `decideCommit`；甘特走 `applyGanttSchedule`；策略走 `commitPackSuggestPolicy` / `commitScheduleSortPolicy`。
- `FactStrip.vue` / `FactDrawer.vue` 调用 `deriveFacts`。
- 约束真源仍在 `src/domain/rule-engine.ts`、`grouping.ts`、`cabinet-task.ts`、`pack-suggest-policy.ts`、`schedule-sort-policy.ts`。

---

## 4. C4-02 中度紧凑（令牌测量 + 设计 §7）

**结论：通过（令牌口径）。视觉截图待 MateBook 补。**

### 4.1 令牌真源 `src/app/styles/tokens.css`

对照详细设计 §7.1 与 §13 示意（侧栏 ~168、表行 ~30）：

| 令牌 | 实现值 | 设计建议 | 示意/本轮必验 | 判定 |
|------|--------|----------|---------------|------|
| `--sidebar-w` | **168px** | 160–180px | ~168 | 命中 |
| `--row-h` | **30px** | 28–32px | ~30 | 命中 |
| `--topbar-h` | **40px** | 表头/顶栏 32–36 另有 `--header-h` | 约 40 | 命中本轮「顶栏 40」 |
| `--header-h` | **34px** | 32–36px | 表头 | 命中 §7.1 |
| `--control-h` | **28px** | 28px | 28 | 命中 |
| `--space-1/2/3` | 4 / 8 / 12px | 4 / 8 / 12px | — | 命中 |
| `--font-xs/sm/md` | 12 / 13 / 14px | 同左 | — | 命中 |
| `--radius` | 4px | 4px | — | 命中 |
| `--gantt-bar-h` | 18px | 甘特条收敛 | — | 命中开发对照文档 |

### 4.2 CSS 接线（非折叠态计算口径）

`src/app/styles/compact.css`：

| 选择器 | 声明 | 期望计算 |
|--------|------|----------|
| `.sidebar` | `width/min-width: var(--sidebar-w)` | **168px**（`.is-collapsed` 才 52px，折叠态除外） |
| `table.data td` | `height: var(--row-h)` | **30px** |
| `table.data th` | `height: var(--header-h)` | **34px** |
| `.topbar` | `height: var(--topbar-h)` | **40px** |
| `.btn` / `.input` / `.select` | `height: var(--control-h)` | **28px** |

颜色语义 `--error` / `--warning` 未改；`.tag` 高度 `--tag-h: 20px`，仅内边距收敛。

入库单测 `change4-vue.test.ts`「C4-02 中度紧凑令牌」断言上述 168 / 30 / 34 / 40 / 28。

### 4.3 未在本 VM 完成的视觉项

- 组柜 `/pack`、进炉计划 `/gantt` **同分辨率前后截图未采**。
- 首屏可见行数/柜卡数未用量测工具记录。
- 可读性（重叠、截断、操作目标）未在真实浏览器扫读。

开发已提供 `docs/验收-变更4-密度对照.md` 作为手工量测清单，**不能替代 MateBook 截图**。本项按执行口径以令牌+§7 对照通过，并标明：**视觉截图待 MateBook 补**。

---

## 5. C4-03 B1–B9 行为零回退

**结论：通过。静态/单测覆盖，UI 路径由 MateBook 短冒烟补。**

覆盖标记：

- **已自动化**：Vitest 直接证明
- **jsdom**：挂载 Vue 应用后点击/查询 DOM
- **仅静态核对**：对照 `planStore` / `.vue` 接线
- **未覆盖需手工**：真实 18080 点击；不阻断本轮（执行口径允许）

| ID | 回归铁律 | 结果 | 覆盖 | 证据 |
|----|----------|------|------|------|
| **B1** | 组柜无 `date`/`shift`；不建 `CabinetTask` | **通过** | 已自动化 + 静态 | `grouping.ts` `draftContent`/`manualPackCabinet` 字面量 `date:null, shift:null, taskId:null`。`change1-grouping.test.ts` C1-37：Content+Trays+OnTray，无 Task。`pack-suggest-policy.test.ts` C3-07 同口径 |
| **B2** | 仅甘特同步才建 Task，写回 `date`/`shift` | **通过** | 已自动化 + 静态 | `change1-grouping.test.ts` C1-23/38：`applyGanttSchedule` 后 `taskId` 有值，`date/shift/seq` 写回。`planStore.syncFurnacePlan` 唯一调用 `applyGanttSchedule` 写 `this.tasks`。UI「同步装炉结果」绑定 `plan.syncFurnacePlan()`。**说明**：`enterGantt()` 进入甘特页会 **静默** `syncFurnacePlan({ silent:true })`，与旧命令式 UI「进进炉计划即建链」同类接线，非本轮新增业务规则；MateBook 短冒烟时按「进入甘特即可见 Task」理解，不必强求必须先点按钮 |
| **B3** | 点行查看 ≠ 勾选 | **通过** | 已自动化 + jsdom + 静态 | domain `focusDemand` 只改 `focusedId`、不改 `checkedIds`；`toggleDemandCheck` 才改勾选（`change1-grouping` C1-42/43）。`PackCabinetView` 行 `@click="plan.focusGroupingDemand"`，checkbox `@click.stop` `@change="plan.checkGroupingDemand"`。jsdom `change1-boot.test.ts`：点 `[data-grp-demand-view="P001"]` 后 checkbox 仍 `checked===false` |
| **B4** | `packSuggestPolicy` 保存后下次自动组柜生效 | **通过** | 已自动化 + 静态 | `commitPackSuggestPolicy` 不改已有 Content（C3-08）。`savePackPolicyFromDraft` 只写 `config.packSuggestPolicy` + `persist()`，toast「将在下次自动组柜生效」，**不**调用 `runAutoPack`。`applyMode` 固定 `nextAutoPackOnly` |
| **B5** | `scheduleSortPolicy` 保存后下次甘特同步生效 | **通过** | 已自动化 + jsdom + 静态 | `commitScheduleSortPolicy` 不重写 tasks/contents（C2-06）。jsdom `change2-ui.test.ts`：改 volume 升降并保存后 localStorage `tasks`/`contents` JSON 全等。`saveSortPolicyFromDraft` 不调用 `syncFurnacePlan`。`applyMode`=`nextSyncOnly`。默认预览 `date↑ · shift↑ · volume↓ · (id)` |
| **B6** | auto 零 error；manual 可带强预警违例 | **通过** | 已自动化 + 静态 | `commit-gate.test.ts` A-AUTO-01/02 拒绝落盘；A-MAN-01 可持久化并 `manualViolation`。组柜 `change1-grouping`：auto 对 `TRAY_OVERFLOW` 拒盘，manual 可盘。工具条双模式绑定 `plan.setScheduleMode` |
| **B7** | 四码保持 | **通过** | 已自动化 | `TRAY_OVERFLOW`：`change1-grouping` 超/未超托盘 + auto 闸门。`REPACK_AFTER_LOAD_COMPLETE`：装填完毕 auto 禁再拼 / manual 强预警。`ON_TRAY_QTY_OVERFLOW`：单柜与跨柜超量 + auto 拒盘。`BOX_LIMIT`：`rule-engine` + `excel-r1-r5` 大箱合计 >280 才报，总箱大但大箱 ≤280 不误报。码名仍在 `ISSUE_CODES` |
| **B8** | localStorage 键 `zhende_sterilization_plan_v2` | **通过** | 已自动化 + 静态 | `STORAGE_KEY` 字面量未改。`plan-store.test.ts` 读写该键；`scheduleMode` / `packSuggestPolicy` / `scheduleSortPolicy` 刷新可恢复（A-MODE-01、C3-09、C2-10）。旧快照缺字段走默认，不丢既有 Content |
| **B9** | Docker `18080→80` 仍可用 | **通过（静态）** | 文件约定 + 入库单测 | 见 §7 C4-05。**Docker 实跑在 MateBook** |

### 5.1 主线六点（提纲 §5.1）

| 主线章节 | 回归主题 | 覆盖铁律 | 本轮执行结论 |
|----------|----------|----------|--------------|
| 第2节 | 待排事实：交期、指定柜、适用规则、排入状态 | B3 入口状态抽查 | **通过（单测）**：`facts.test.ts` DESIGNATED / D002 目标 / 大箱芯片；`DemandPoolView` 使用 `FactStrip`。**UI 路径由 MateBook 短冒烟补** |
| 第3节 | 组柜双入口、点行/勾选、无日期班次筛选 | B1、B3 | **通过（单测+jsdom）**：`change1-boot` 可见选柜/选需求、无 `#grpShiftTabs`；点行不勾选 |
| 第4节 | 托盘分层、超托盘、分量超量 | B6、B7 | **通过（单测）**：分层 Tray/OnTray；柜17 口径 `TRAY_OVERFLOW`；`ON_TRAY_QTY_OVERFLOW`。jsdom 打开组柜可见「超托盘」芯片 |
| 第5节 | 建议策略保存及下次自动组柜 | B1、B4 | **通过（单测+jsdom）**：面板契约 + 保存不改载荷 |
| 第6节 | 甘特同步建 Task、排序下次生效 | B1、B2、B5 | **通过（单测+jsdom）**：同步建链；保存排序 Task 不变 |
| 第7节 | auto 拒硬错、manual 强预警及再拼 | B6、B7、B8 | **通过（单测）**：闸门与 hydrate。真实双模式点击 **MateBook 补** |

---

## 6. C4-04 测试与生产构建

**结论：通过。**

| 命令 | 退出码 | 记录 |
|------|--------|------|
| `npm test` | **0** | **183 / 183 passed**（17 files） |
| `npm run build` | **0** | `vue-tsc --noEmit && vite build`；产出 `dist/` |

无 TypeScript/Vue 类型错误。无本轮为「修绿」改动的业务代码或测试断言。

---

## 7. C4-05 Docker / SPA fallback

**结论：通过（静态审阅）。Docker 实跑在 MateBook。**

本 VM：`docker: command not found`，未跑 `docker compose up -d --build`，未 `curl http://127.0.0.1:18080/`。

| 检查项 | 文件 | 实际 |
|--------|------|------|
| 端口映射 | `docker-compose.yml` | `"18080:80"`，服务 `zd-sterilization-plan`，无 8088 原型 |
| SPA fallback | `nginx.conf` | `try_files $uri $uri/ /index.html`；`listen 80`；`root /usr/share/nginx/html` |
| 构建链 | `Dockerfile` | multi-stage：`npm ci` → `npm run build` → nginx 拷贝 `dist`；`EXPOSE 80` |
| 入库断言 | `change4-vue.test.ts` | 断言 compose `18080:80`、nginx `try_files`、Dockerfile `npm ci` + `npm run build` |

MateBook 必做（开发 PR 亦已写明，且须 `--build` 避免旧命令式镜像）：

```bash
docker compose up -d --build
curl -sI http://127.0.0.1:18080/
# 抽查刷新：/pack /gantt /pool /validation 不 404
```

---

## 8. 柜8 演示冲突（不当失败）

按提纲 §8 / 设计 §11：柜8 冲突是故意演示数据，**不能据此判失败**。

本轮核对：

- 演示种子 `getDemoFurnaceSeed()` 含 `{ cabinetId: '柜8', lines: ['P013'] }`；P013 为手术衣、`allowed: ['柜8']`，属演示载荷而非本轮回归失败条件。
- 单测里「柜8」大量用作 `BOX_LIMIT` / `CABINET_MISMATCH` **夹具柜号**（把 D002 行塞进柜8 等负例），与「演示冲突不当失败」规则一致：这些是规则引擎正例/负例，不是把柜8 演示态当缺陷。

本报告 **未** 因柜8 相关校验码或种子数据判 C4 失败。

---

## 9. 必改项 / 风险项

### 9.1 必改项

无。

### 9.2 风险项（不挡「带风险通过」）

| ID | 级别 | 说明 | 是否硬失败 |
|----|------|------|------------|
| R1 | 风险 | **视觉截图待 MateBook 补**：无同分辨率组柜/甘特前后对照，无首屏行数量测。令牌与 CSS 已对齐 §7 | 否（执行口径允许令牌代替） |
| R2 | 风险 | **B1–B9 UI 路径由 MateBook 短冒烟补**：未在 18080 手工走主线第2–7节。domain + jsdom 已覆盖铁律 | 否 |
| R3 | 风险 | **Docker 实跑在 MateBook**：本 VM 无 daemon。文件层 18080→80 + `try_files` 已审 | 否 |
| R4 | 信息 | 进入 `/gantt` 时 `enterGantt()` 静默同步建 Task，与「必须先点同步按钮」的字面手工步骤略有差别；属既有进炉页接线，MateBook 按进入甘特即见 Task 验收即可 | 否 |
| R5 | 信息 | 甘特轨道 `--gantt-row-h: 44px` 高于表体 30px；条高 18px 已收敛。不以甘特轨道高等于表行高为失败 | 否 |
| R6 | 信息 | `npm audit` 2 moderate、`whatwg-encoding` deprecated、部分传递依赖 EBADENGINE——工程卫生，非功能失败 | 否 |

---

## 10. 对照详细设计 v1.0（静态）

| 设计点 | 实现 | 结论 |
|--------|------|------|
| Vue 3 Composition / `<script setup>` | 页面与布局均为 setup；入口 `createApp` | 成立 |
| Vite 6 + `@vitejs/plugin-vue` + `vue-tsc` 参与 build | `vite.config.ts`、`package.json` scripts | 成立 |
| vue-router 4 + Pinia | router 4.6.4 / pinia 3.0.4 | 成立 |
| history + nginx fallback | `createWebHistory` + `try_files` | 成立 |
| 未引入 Element Plus | 依赖表无该包 | 成立 |
| `.vue` 不直接 localStorage | grep + 单测 | 成立 |
| `.vue` 不复制约束 | 调用 domain / planStore | 成立 |
| 键 `zhende_sterilization_plan_v2` | `STORAGE_KEY` 未改名 | 成立 |
| 旧 `src/ui` 合并前删除 | 目录不存在 | 成立 |
| 中度令牌 168 / 30 / 28 | tokens.css + compact.css | 成立 |
| Docker 18080:80 不带原型 | compose 仅一服务 | 成立 |
| 8088 原型不迁 Vue | 本 PR 未改原型站 | 成立（非本仓强制项） |

---

## 11. 给 MateBook 的短补清单（不阻塞本报告结论）

1. **C4-02**：同分辨率、缩放 100%，组柜 + 进炉计划各一张迁移后截图；DevTools 量 `.sidebar`、`table.data td`、`.topbar`；记录首屏行数。对照 `docs/验收-变更4-密度对照.md`。
2. **C4-03**：`http://127.0.0.1:18080/` 走主线第2–7节（柜3 半满、柜14 待入炉、柜16 装填完毕/分量超量、柜17 超托盘）。柜8 冲突只记录、不判失败。
3. **C4-05**：`docker compose up -d --build` 后首页 HTTP 200；硬刷新 `/pack` `/pool` `/gantt` `/validation` 不 404。

---

*本报告仅新增于 `docs/验收报告-变更4-PR22.md`，未修改业务功能代码或测试断言。被测提交保持 `b9c39924abbbf7858ac0e91193f8535056ee9b9a`。*
