# 振德医疗 · 灭菌排产 · 变更-2 · 正式验收报告（PR #18）

| 项 | 内容 |
|----|------|
| 验收对象 | [PR #18](https://github.com/lujie1980-oss/ZD-SterilizationPlan/pull/18) · 分支 `cursor/schedule-sort-policy-4ab8` |
| 被测提交 | `7d8f8cf8271c28c4c479d92bd8c9ecfcd8d052ab`（`feat: make Gantt entry sort configurable (change-2)`） |
| 相对 GitHub `main`（`c3539ab`） | `c3539ab`…`7d8f8cf` · **+1117 / −38**（13 files） |
| 计划依据 | 《变更-2 测试提纲》v0.1；《详细设计》v1.0（`plan v2.config.scheduleSortPolicy`；due/urgent/fillRate 默认关） |
| 执行日 | 2026-09-16 |
| 执行方式 | `npm install` → `npm test` → `npm run build` + 领域单测映射 + 源码静态核对 + **仓库外** vitest 探针（C2-01～09，16 项）+ **仓库外** jsdom 面板/保存路径（C2-01/02/06/09）；**未做浏览器手工点击** |
| 验收角色 | 验收执行助手（**不改业务功能代码**；本文件为唯一新增交付） |

---

## 1. 总结论

**通过。** 变更-2 在 `7d8f8cf` 上甘特入炉排序可配且可区分；铁律（按柜建链 · 不改交期 · 组柜不建 Task）未被配掉；保存后仅下次甘特同步生效。`packSuggestPolicy` 变更-3 默认真源仍在。

| 用例ID | 结果 |
|--------|------|
| C2-01 非法配置拒存 | **通过** |
| C2-02 恢复默认 = date↑ shift↑ volume↓ id↑ | **通过** |
| C2-03 调 volume 升降/换序 → 下次同步 seq 可区分 | **通过** |
| C2-04 启用 due 置前 → 可区分 | **通过** |
| C2-05 urgent / fillRate 可区分 | **通过** |
| C2-06 仅保存不同步 → Task 链不变 | **通过** |
| C2-07 不改 due；组柜无 Task | **通过** |
| C2-08 按柜建链 | **通过** |
| C2-09 build 绿；packSuggestPolicy 默认仍在 | **通过** |

`npm test`：**176 / 176 通过**。`npm run build`：退出码 0。无必改项。

---

## 2. 环境与命令结果

| 项 | 结果 |
|----|------|
| 工作目录 | `/workspace` |
| HEAD（被测） | `7d8f8cf8271c28c4c479d92bd8c9ecfcd8d052ab` |
| Node | v22.14.0 |
| npm | 10.9.7 |
| OS | Linux 6.12.94+ |
| 命令顺序 | `npm install` → `npm test` → `npm run build` |
| GitHub Checks | 该分支当时 **no checks reported**（不作为本轮功能失败） |

### 2.1 `npm install`

- **退出码**：0
- 新增 91 packages，审计 92 packages（约 2s）
- 弃用警告：`whatwg-encoding@3.1.1`（jsdom 传递依赖）
- npm audit：2 moderate；**不作为功能验收失败**

### 2.2 `npm test`（`vitest run` v3.2.7）

- **退出码**：0
- **Test Files 16 passed / Tests 176 passed (176)**，Duration ~4.29s

| 文件 | 用例数 | 本轮相关性 |
|------|--------|------------|
| `src/domain/__tests__/schedule-sort-policy.test.ts` | 15 | C2-01～09、C2-11/12 |
| `src/domain/__tests__/change2-ui.test.ts` | 2 | 面板 HTML 契约 + jsdom：保存不同步 / 恢复默认 |
| `src/domain/__tests__/plan-store.test.ts` | 16 | C2-10 / C2-10b 持久化（P1） |
| `src/domain/__tests__/pack-suggest-policy.test.ts` | 17 | C2-09 变更-3 回归同源 |
| `src/domain/__tests__/change1-grouping.test.ts` | 31 | C2-07 同源：组柜无 Task |
| `src/domain/__tests__/entry-scheduler.test.ts` | 9 | 进炉排序接线未回退 |
| 其余（commit-gate / excel / rule / facts / change1-ui / split / min-load / suggest / boot / export） | 86 | 变更-1 / 变更-3 / 迭代 A 未回退 |

### 2.3 `npm run build`（`tsc --noEmit && vite build`）

- **退出码**：0
- Vite v6.4.3，32 modules transformed，built in 429ms
- 产物：`dist/index.html` 17.33 kB gzip 4.66 kB；`dist/assets/index-FBVVzWIS.css` 22.02 kB gzip 4.95 kB；`dist/assets/index-B5Rns2Wl.js` 126.96 kB gzip 38.51 kB

---

## 3. 铁律核对（硬失败条件）

详细设计 §2 / 提纲 T3–T4：按柜建链、不改 due、组柜不建 Task；非法拒存；恢复默认 = 现行四键；下次同步才生效。

| 铁律 | 结论 | 量化 / 源码证据 |
|------|------|-----------------|
| H1 按柜建链，跨柜不混排 | **成立** | `orderContents` 遇多柜抛 `SORT_POLICY_MIXED_CABINET`。`applyGanttSchedule` 按 `cabIds` 分别 `buildTaskChain`。探针：柜9 / 柜20 各自 seq=`[1,2]`，prev/next 不跨柜 |
| H2 不得改写 `StockLine.due` | **成立** | `cabinet-task.ts` 无 `.due =`。探针：组柜后同步，行 `XA.due` 仍为 `2026-07-29` |
| H3 组柜不建 Task | **成立** | `grouping.ts` `draftContent` / `manualPackCabinet` 字面量 `date: null, shift: null, taskId: null`。探针：`autoPackCabinet` 产物三者皆 null；`applyGanttSchedule` 才写 `taskId` |
| H4 键列表合法；非法拒存 | **成立** | `validateScheduleSortPolicy` / `commitScheduleSortPolicy` 五码：`SORT_POLICY_EMPTY` / `UNKNOWN_KEY` / `DUP_KEY` / `CORE_DISABLED` / `BAD_DIR`。失败不改 `previous` |
| H5 恢复默认 = 现行四键真源 | **成立** | `restoreDefaultScheduleSortPolicy` → 生效 `date↑ → shift↑ → volume↓ → id↑`；可选 due/urgent/fillRate 关。jsdom「恢复默认」预览同上 |
| H6 系统末键 id asc | **成立** | `effectiveKeys` 过滤用户 id 后再追加 `{ code:'id', direction:'asc' }`。C2-11：同体积同日同班 `AA` 先于 `ZZ` |
| 生效 `nextSyncOnly` | **成立** | coerce / commit 均写死 `applyMode:'nextSyncOnly'`。`saveSortPolicyFromDraft` 只改 config + `persist()`，**不**调用 `syncFurnacePlan` |

---

## 4. P0 逐条（C2-01～09）

覆盖标记：

- **已自动化覆盖**：Vitest 直接证明预期
- **探针核对**：仓库外 vitest（不入库）
- **jsdom 核对**：仓库外 / 入库 jsdom 点保存/恢复（不入库探针 + `change2-ui.test.ts`）
- **仅静态核对**：对照 `app.ts` / `furnace-plan-view.ts` / 领域模块
- **未覆盖需手工**：需真实浏览器点击；不阻断本轮

| 用例ID | 结果 | 覆盖 | 证据 | 备注 |
|--------|------|------|------|------|
| **C2-01** | **通过** | 已自动化 + 探针 + jsdom + 静态 | `commitScheduleSortPolicy` 对空键 / 全关 / 关 date\|shift\|volume / 未知码 `fooBar` / 重复 date / `direction='up'` **全部拒存**，错误码分别为 `SORT_POLICY_EMPTY` / `CORE_DISABLED` / `UNKNOWN_KEY` / `DUP_KEY` / `BAD_DIR`；`previous` JSON 全等。合法 volume→asc 可存且 `applyMode=nextSyncOnly`。jsdom：date/shift/volume **无**启用开关（文案「必选」），due/urgent/fillRate 有 checkbox。UI 保存失败走 `toast(code：message)` 且不写 config | UI 控件构造不出空键/未知码/关核心键；保存闸门兜住 |
| **C2-02** | **通过** | 已自动化 + 探针 + jsdom | 从「due 置顶 + volume 升序 + fillRate 开」恢复后：`effectiveKeys` = `date:asc, shift:asc, volume:desc, id:asc`；预览 `date↑ · shift↑ · volume↓ · (id)`；可选三键 `enabled=false`；`id=default`、`name=现行四键（默认）`、`nullDatePolicy=treatAsLatest`、`applyMode=nextSyncOnly`。jsdom「恢复默认」后预览同上 | 与设计 §3.4 一致 |
| **C2-03** | **通过** | 已自动化 + 探针 | 不同日：默认 sync seq **CA→CB**；volume 提到 date 前 → **CB→CA**。同日同班仅改 volume 升降：比较器 `desc=CB→CA` vs `asc=CA→CB`。同日同班 auto 同步因同槽重叠拒写（既有进炉闸门），不挡「可区分」 | 换序路径已用真实 `applyGanttSchedule` 写 seq |
| **C2-04** | **通过** | 已自动化 + 探针 | 启用 due 并置 **date 前**（亦在 volume 前）：默认 **CA→CB**，due 置顶后 **CB→CA**（交期 07-25 紧于 08-30）。探针补：due **仅**插在 volume 前（生效 `date→shift→due→volume→id`）时，同日同班比较器同样 **CA→CB** vs **CB→CA** | 入库单测用「due 置顶」满足「置于 volume 前」且可同步；见 §7 |
| **C2-05** | **通过** | 已自动化 + 探针 | urgent desc 置 date 前：默认 CA→CB，加急 CB 提前。fillRate desc 置 date 前：体积 80/额定 100 的 CB 提前（`fillRateOf` 按体积/柜额定，非 Content.fillRate 字段）。探针均经 `applyGanttSchedule` 对照 seq | 各至少一例可区分 |
| **C2-06** | **通过** | 已自动化 + 探针 + jsdom + 静态 | `commitScheduleSortPolicy` 只返回新 policy。UI `saveSortPolicyFromDraft` 写 `config.scheduleSortPolicy` 再 `persist()`，不跑 `syncFurnacePlan`。jsdom：点 volume「升」→ 保存后 `direction=asc`，`tasks` / `contents` JSON **全等**；toast「将在下次甘特同步生效」 | 生效时机 = `nextSyncOnly` |
| **C2-07** | **通过** | 已自动化 + 探针 + 静态 | `autoPackCabinet`：`taskId/date/shift` 皆 null。随后 `applyGanttSchedule` 才有 `taskId`。`StockLine.due` 同步前后均为 `2026-07-29`。`cabinet-task.ts` 无 due 写回 | 变更-1 边界未破 |
| **C2-08** | **通过** | 已自动化 + 探针 | 柜9 两 Content + 柜20 两 Content 一次同步：两柜 seq 均为 `[1,2]`；柜9 的 prev/next 不含柜20 task id；混柜 `orderContents` 抛 `SORT_POLICY_MIXED_CABINET` | 按柜建链 |
| **C2-09** | **通过** | 已自动化 + 探针 + jsdom + 命令 | `npm test` 176 绿；`npm run build` 退出码 0。`defaultAppConfig().packSuggestPolicy`：`preset=fillFirst`、`fillMode=fillOneFirst`、`targetFillRate=0.8`、`dueWindowDays=3`、交期簇关、`applyMode=nextAutoPackOnly`。jsdom 保存排序策略后 `packSuggestPolicy` JSON 不变 | 变更-3 真源仍在 |

---

## 5. 提测检查清单（提纲 §3）

| 项 | 结论 |
|----|------|
| UI：进炉计划「排序策略」面板含四键调序/升降；due/urgent/fillRate 可开；保存/恢复默认；铁律提示 | **通过**（HTML 契约 + jsdom：预览 `date↑ · shift↑ · volume↓ · (id)`；铁律条含按柜建链、不改交期、组柜不建 Task、系统末键 id） |
| 非法配置拒存；恢复默认正确 | **通过**（C2-01 / C2-02） |
| 调策略后下次同步 seq 可区分 | **通过**（C2-03 / C2-04 / C2-05） |
| 仅保存不同步 → Task 链不变 | **通过**（C2-06） |
| 不改 due；组柜无 Task；按柜建链 | **通过**（C2-07 / C2-08） |
| 变更-3 默认策略回归 | **通过**（C2-09） |

---

## 6. P1 抽样（非本轮必过）

| 用例 | 结论 |
|------|------|
| C2-10 刷新后从 plan v2 恢复 | **已自动化通过**：volume 置顶 asc、due 启用读回；`applyMode=nextSyncOnly`；`packSuggestPolicy.preset` 仍为 fillFirst |
| C2-10b 旧 plan 无字段 | **已自动化通过**：缺省视为现行四键；既有 `P001` 不丢；packSuggestPolicy 仍默认 |
| C2-11 系统强制末键 id | **已自动化通过**：同日同班同体积，`AA` 先于 `ZZ` |
| C2-12 未排 Content 靠后 | **已自动化通过**：`date=null` 的 UNSCHED 排在已排 SCHED 之后（`treatAsLatest`） |

---

## 7. 残留与建议（不破铁律、不挡通过）

| ID | 级别 | 说明 | 是否硬失败 |
|----|------|------|------------|
| R1 | 信息 | 本轮 **无浏览器 E2E**：拖拽调序像素/焦点、真实点「同步装炉结果」未点。jsdom 已覆盖保存拒存路径之外的「升序保存 / 恢复默认 / Task 不变」 | 否 |
| R2 | 信息 | C2-04 入库单测把 due 置 **date 前**（比提纲「volume 前」更靠前），以便不同日样本能同步写 seq。due 仅插在 volume 前时，需同日同班比较器才可区分；该样本 auto 同步会因时段重叠拒写（既有闸门）。比较器层已证明 due-before-volume 可翻转 | 否 |
| R3 | 信息 | C2-01 未知码/空键/关核心键无法用面板控件构造（核心三键无启用开关）。保存闸门 `commitScheduleSortPolicy` 仍全覆盖 | 否 |
| R4 | 信息 | C2-06 领域单测只断言 `commitScheduleSortPolicy` 不接收 contents/tasks（函数签名如此）。**Task 链不变**的有效证据是 jsdom：保存后 localStorage `tasks`/`contents` JSON 全等 | 否 |

无必改项。UAT 建议：进炉计划改 volume 升降 → 保存（甘特条不动）→ 再「同步装炉结果」看同柜 seq 变化 → 恢复默认。

---

## 8. 对照详细设计 v1.0（静态）

| 设计点 | 实现 | 结论 |
|--------|------|------|
| 落点 `config.scheduleSortPolicy` | `AppConfig` + `mergeConfig` / `persistableConfig` / plan-store | 成立 |
| 核心 date/shift/volume 恒开；due/urgent/fillRate 默认关 | `defaultScheduleSortPolicy`；UI 核心键「必选」 | 成立 |
| id 不展示，生效序列末尾强制 id asc | 面板 `k.code !== 'id'`；`effectiveKeys` 追加 | 成立 |
| 校验 5 错误码、非法拒存 | `validateScheduleSortPolicy` / `commitScheduleSortPolicy` | 成立 |
| §3.4 默认真源 | `date↑ → shift↑ → volume↓ → id↑` | 成立 |
| `applyMode` 固定 `nextSyncOnly` | coerce / commit 均写死 | 成立 |
| `nullDatePolicy` 固定 `treatAsLatest` | coerce 写死；未排靠后 | 成立 |
| 按柜 `orderContents` → `rebuildCabinetTaskChain` | `applyGanttSchedule` 分柜 `buildTaskChain` | 成立 |
| 同步不改 `StockLine.due` | `cabinet-task.ts` 无 due 写回 | 成立 |
| 组柜不写 date/shift、不建 Task | grouping draft 字面量 null | 成立 |
| UI：生效预览 / 调序升降下移 / 可选开关 / 保存 / 恢复默认 / 铁律条 | `furnaceSortPolicyPanelHtml` + `app.ts` | 成立 |
| 与 `packSuggestPolicy` 字段互不覆盖 | `defaultAppConfig` 两字段并存；保存排序不改 pack | 成立 |

---

## 9. 附录：保存 vs 甘特同步

| UI 动作 | 是否改 Task 链 | 策略 |
|---------|----------------|------|
| 保存排序策略 | **否**（只写 config + persist） | 非法则 toast `SORT_POLICY_*`，旧策略不变 |
| 恢复默认 | **否** | 写回 §3.4，`version++` |
| 同步装炉结果 / 生成入炉顺序 | **是** | 读当前 `config.scheduleSortPolicy`；按柜重排；写回 date/shift/seq；不改 due |
| 自动/手动组柜 | 不建 Task | **不**走排序策略建链 |

---

*本报告仅新增于 `docs/验收报告-变更2-PR18.md`，未修改业务功能代码。被测提交保持 `7d8f8cf8271c28c4c479d92bd8c9ecfcd8d052ab`。*
