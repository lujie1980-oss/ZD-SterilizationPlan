# 振德医疗 · 灭菌排产 · 变更-4 + 变更-5 · PR #22 联合验收报告

| 项 | 内容 |
|----|------|
| 验收对象 | [PR #22](https://github.com/lujie1980-oss/ZD-SterilizationPlan/pull/22) · 分支 `cursor/vue3-compact-ui-c8c0` |
| 被测提交 | `97e0ec37f4b49d3fc39f0382a06d22d7f84c887d`（`feat: reorganize Vue shell into five-level IA (change-5)`） |
| 其上 Vue3 提交 | `b9c39924abbbf7858ac0e91193f8535056ee9b9a`（`feat: migrate SPA to Vue 3 compact UI (change-4)`） |
| 相对 `main@7799527` | 2 commits · 48 files · **+4261 / −2670** |
| 计划依据 | 《变更-4 Vue3 紧凑 UI 测试提纲》v0.1；《变更-5 信息架构重组测试提纲》v0.1；《变更-5 导航映射》v1.0 |
| 执行日 | 2026-09-17 |
| 执行方式 | `npm ci` → `npm test` → `npm run build` + 源码/路由/令牌/compose **静态核对** + jsdom 自动化（C4/C5）。**本 VM 无 Docker daemon**，C4-05 仅静态审阅，未 `compose up` / 未 curl `18080`。**未做浏览器手工点击与密度实测截图。** |
| 验收角色 | 测试侧联验执行者（**不改 `src/`、不修测试断言**；本文件为唯一新增交付） |

---

## 1. 总结论

**带风险通过。**

在本轮联验口径内（C4 抽查确认仍成立 + C5 重点），C4-01～C5-05 **均未发现阻断失败**。`npm test` **188 / 188 通过**（与开发宣称一致）。`npm run build`（`vue-tsc --noEmit && vite build`）退出码 0。柜8 演示冲突属故意数据，**不当失败**。

风险不构成 C4/C5 用例失败，但必须带着走：

1. **本 VM 不能实起 Docker**：C4-05 只核对 `docker-compose.yml` `18080:80`、`nginx.conf` `try_files`、Dockerfile 构建链；**未**验证首页 HTTP 200 与 SPA 刷新不 404。
2. **C4-02 未做 18080 DevTools 实测与前后截图**：令牌已落地 168 / 30 / 40 / 28，CSS 已绑定；肉眼可扫读与首屏行数对照留待本机/MateBook。
3. **结果发布「已排期列表」仍嵌入旧工作台操作条**（双模式 / 分配到选中炉 / 建议拼炉 / 拆炉等）。结果日与 CSV 已从全局顶栏移入 `/release`，C5-01～C5-05 仍成立，但壳层未完全收敛为「纯浏览 + 发布」。

无必改项。不改业务代码。

---

## 2. 环境与命令结果

| 项 | 结果 |
|----|------|
| 工作目录 | `/workspace` |
| 被测 HEAD | `97e0ec37f4b49d3fc39f0382a06d22d7f84c887d` |
| Node | v22.14.0 |
| npm | 10.9.7 |
| OS | Linux 6.12.94+ x86_64 |
| 命令顺序 | `npm ci` → `npm test` → `npm run build` |
| Docker | **不可用**（`docker` / `docker compose` 无输出） |
| GitHub Checks | PR #22 `statusCheckRollup` **空**（不作为本轮功能失败） |

### 2.1 `npm ci`

- **退出码**：0
- 新增 159 packages，审计 160 packages（约 2s）
- 警告：`abbrev@5.0.0` / `nopt@10.0.1` `EBADENGINE`（要 Node `^22.22.2`，当前 22.14.0）；`whatwg-encoding@3.1.1` deprecated
- npm audit：2 moderate；**不作为功能验收失败**

### 2.2 `npm test`（`vitest run` v3.2.7）

- **退出码**：0
- **Test Files 18 passed / Tests 188 passed (188)**，Duration ~3.84s

| 文件 | 用例数 | 本轮相关性 |
|------|--------|------------|
| `src/app/__tests__/change4-vue.test.ts` | 7 | C4-01 / C4-02 令牌 / C4-05 静态 / 运行时路由 |
| `src/app/__tests__/change5-ia.test.ts` | 5 | C5-01～C5-05 jsdom |
| `src/domain/__tests__/change1-grouping.test.ts` | 31 | B1–B3、B6–B7（Task / 点行勾选 / 热修码闸门） |
| `src/domain/__tests__/change1-boot.test.ts` | 1 | 组柜 jsdom：双入口、点行≠勾选、超托盘芯片 |
| `src/domain/__tests__/change1-ui.test.ts` | 5 | 组柜无班次筛选、REPACK 文案、建议策略面板 |
| `src/domain/__tests__/change2-ui.test.ts` | 2 | B5：排序策略面板挂入炉计划；保存不改 Task |
| `src/domain/__tests__/schedule-sort-policy.test.ts` | 15 | B2 / B5 |
| `src/domain/__tests__/pack-suggest-policy.test.ts` | 17 | B1 / B4 / B7 |
| `src/domain/__tests__/commit-gate.test.ts` | 15 | B6；柜8 `CABINET_MISMATCH` 故意数据 |
| `src/domain/__tests__/plan-store.test.ts` | 16 | B8 `zhende_sterilization_plan_v2`；策略持久化 |
| `src/domain/__tests__/rule-engine.test.ts` | 21 | B7 BOX_LIMIT 口径2 |
| `src/domain/__tests__/export-csv.test.ts` | 2 | C5-03：`date=null` 不进日计划 CSV |
| 其余 domain 单测 | 47 | 一期/二期/变更-1～3 回归，未因壳层迁移变红 |

### 2.3 `npm run build`（`vue-tsc --noEmit && vite build`）

- **退出码**：0
- Vite v6.4.3，101 modules transformed，built in 1.15s
- 产物：`dist/index.html` 0.67 kB；`dist/assets/index-Dq73_7MU.css` 24.36 kB gzip 5.41 kB；`dist/assets/index-CxWz1I7w.js` 193.53 kB gzip 68.19 kB；路由懒加载 chunk 含 `MasterDataView` / `DemandPoolView` / `PackCabinetView` / `GanttPlanView` / `ReleaseView` 等

---

## 3. C4 逐项（抽查确认仍成立）

| ID | 结论 | 证据 | 缺口 |
|----|------|------|------|
| **C4-01** | **通过** | `package.json`：`vue` `^3.5.22`、`vue-router`、`pinia`、`@vitejs/plugin-vue`；`src/main.ts` `createSterilizationApp` + `createApp`；无 `src/ui`。关键页均为 `.vue`：`DemandPoolView` / `PackCabinetView` / `GanttPlanView` / `ReleaseView`（校验中心改为结果发布内嵌 `ValidationCenterView.vue`，不再占一级路由）。`.vue` 不含 `localStorage`；hydrate/save 经 `planStore` → `persistence/plan-store-v2.ts`。`change4-vue.test.ts` 4 条静态 + 1 条运行时路由绿 | C4 原提纲 `/pool` `/validation` 已由 C5 改为 `/demand` `/release`；旧 path 有 redirect，见 §5 |
| **C4-02** | **通过（令牌）** | `src/app/styles/tokens.css`：`--sidebar-w: 168px`、`--row-h: 30px`、`--topbar-h: 40px`、`--control-h: 28px`（另 `--header-h: 34px`，落在原提纲表头 32–36）。`compact.css`：`.sidebar { width: var(--sidebar-w) }`、`table.data td { height: var(--row-h) }`、`.topbar { height: var(--topbar-h) }`、`.btn` / `.input` 用 `--control-h`。`change4-vue.test.ts` C4-02 绿 | **未**在 18080 用量尺；**无**组柜/甘特同分辨率前后截图。可扫读只能静态推断 |
| **C4-03** | **通过（单测/静态）** | B1–B9 对应单测/契约均仍在且本轮全绿，见下表。本联验口径为「单测/静态仍在」，**不是** 18080 主线第 2–7 节手工六路径 | 未在 Docker 地址走主线手工 B1–B9 |
| **C4-04** | **通过** | `npm test` 188 passed；`npm run build` 退出码 0；无 `vue-tsc` 报错；产出 `dist/` | — |
| **C4-05** | **通过（静态）** | `docker-compose.yml`：`"18080:80"`。`nginx.conf`：`try_files $uri $uri/ /index.html`。`Dockerfile`：`npm ci` + `npm run build`，nginx 拷 `dist`。`change4-vue.test.ts` C4-05 绿 | **本 VM 无 Docker**，未 `--build`、未 curl、未刷新 `/master` `/demand` `/pack` `/gantt` `/release` |

### 3.1 B1–B9 单测/静态映射（C4-03）

| ID | 铁律 | 本轮证据 | 结论 |
|----|------|----------|------|
| B1 | 组柜无 `date`/`shift`；不建 `CabinetTask` | `change1-grouping` C1-11/22/37；`pack-suggest-policy` C3-07；组柜工具条无班次筛选（`change1-ui` / `change1-boot`） | 仍在 |
| B2 | 仅甘特同步才建 Task，写回 `date`/`shift` | `change1-grouping` C1-23/38 `applyGanttSchedule`；`planStore.syncFurnacePlan` → `applyGanttSchedule`；`GanttPlanView` `#btnFpSync` | 仍在 |
| B3 | 点行查看 ≠ 勾选 | `focusDemand` 只改 `focusedId`；`change1-grouping` C1-42/43；`change1-boot` jsdom：点 `P001` 行后 checkbox 仍未勾；`PackCabinetView` checkbox `@click.stop` | 仍在 |
| B4 | `packSuggestPolicy` 保存后下次自动组柜生效 | `pack-suggest-policy` C3-08 仅保存不改载荷；C3-03/04 策略可区分；`PackPolicyPanel` + `#btnSavePackPolicy` 仍在 `/pack` | 仍在 |
| B5 | `scheduleSortPolicy` 保存后下次甘特同步生效 | `schedule-sort-policy` C2-06 保存不改 Task；C2-03 下次同步 seq 可区分；`change2-ui` jsdom 面板在入炉计划 | 仍在 |
| B6 | auto 零 error；manual 可带强预警违例 | `commit-gate` A-AUTO/A-MAN；`change1-grouping` auto 拒 TRAY/再拼/分量超量，manual 可落盘 + 手工违例；`/pack` `#grpScheduleModeTabs` `data-mode=auto\|manual` | 仍在 |
| B7 | `TRAY_OVERFLOW` / `REPACK_AFTER_LOAD_COMPLETE` / `ON_TRAY_QTY_OVERFLOW` / `BOX_LIMIT` | domain 单测仍覆盖四码；组柜页 `GROUPING_ISSUE_CODES` + 本批摘要展示 `i.code`；`change1-boot` 可见「超托盘」 | 仍在 |
| B8 | localStorage 键 `zhende_sterilization_plan_v2` | `config-defaults.ts` `STORAGE_KEY`；`plan-store.test.ts` round-trip；`.vue` 不直接读写 | 仍在 |
| B9 | Docker `18080→80` | 文件约定仍在（同 C4-05）；本 VM 未实起 | 静态仍在 |

---

## 4. C5 逐项（重点）

| ID | 结论 | 证据 |
|----|------|------|
| **C5-01** | **通过** | `AppShell.vue` `nav` 唯一真源，五项固定顺序：基础数据维护 `/master` → 待排产需求确认 `/demand` → 组柜优化 `/pack` → 入炉计划 `/gantt` → 结果发布 `/release`。无「校验中心」「已排期浏览」「待灭菌可排池」「进炉计划」等旧名一级项；侧栏文案是「组柜优化」而非「组柜」。`change5-ia.test.ts` C5-01：点击五项后顺序不变。路由 `meta.title` 与菜单一致 |
| **C5-02** | **通过** | 全局 `.topbar` 仅 `#topbarTitle` + 空 `.topbar-actions`，无「结果日」、无「导出日计划 CSV」、无 `#topResultDate`。jsdom 对 `/master` `/demand` `/pack` `/gantt` `/release` 五页断言顶栏不含上述文案；非 `/release` 页不存在 `#btnExport` / `#releaseResultDate` |
| **C5-03** | **通过** | 仅 `ReleaseView.vue` 页内工具区：`#releaseResultDate`「结果日」+ `#btnExport`「导出日计划 CSV」+ 可选班次。`currentFurnaces(furnaces, date, shift)` 按 **炉次 `date`（上线日）** 过滤，不是交期。jsdom：`P001.due !== dayA` 时按交期筛选看不到已按 `dayA` 排期的炉；`date=null` 炉不进 `#furnaceGrid`、不进默认 CSV。`export-csv` C1-02 同样排除未排期。校验 Tab `collectReleaseIssues()` 只用已排期炉次 + 当日冲突，排除 `date=null` 组柜本批码 |
| **C5-04** | **通过** | `/pack`：双入口、`PackPolicyPanel`、`#grpScheduleModeTabs` auto/manual、本批校验码。`/gantt`：甘特、`SortPolicyPanel`、`#btnFpSync`。旧 path redirect 见 §5。热修码：domain 四码测试仍绿；组柜摘要与结果发布校验段展示 `i.code`（含 `TRAY_OVERFLOW` 等）。主数据 `/master/cabinets|processes|boxspecs` 可达 |
| **C5-05** | **通过** | 侧栏无「校验中心」、无 `href="/validation"` 一级链。`/validation` redirect → `/release?tab=issues`。结果发布有「已排期列表 / 校验问题」Tab，issues 挂载 `#page-validation`。组柜 `#grpValidationSummary` `data-testid="pack-validation-summary"`，文案「不占一级菜单」，链到 `/release?tab=issues` |

### 4.1 结果发布页内契约（对照导航映射 §4）

```
[ 结果日: date ]  [ 白班/夜班 ]  [ 导出日计划 CSV ]
------------------------------------------------
Tab：已排期列表 | 校验问题（筛该结果日 / 上线日）
```

与实现一致。班次为页内可选过滤（`currentFurnaces` 同时吃 `date`+`shift`），符合映射「可选：班次」。默认 `export.blockOnError === false`，C5-03 导出用例可下载。

---

## 5. 路由与旧 path

| path | 处理 | 落地 |
|------|------|------|
| `/` | redirect | `/demand`（默认待排产需求确认） |
| `/master` | 一级 | 基础数据维护；默认再 redirect `/master/cabinets` |
| `/master/cabinets` | 子路由 | 灭菌柜 / 托盘 |
| `/master/processes` | 子路由 | 工艺与允许柜 |
| `/master/boxspecs` | 子路由 | 箱规 |
| `/demand` | 一级 | 待排产需求确认（原可排池） |
| `/pack` | 一级 | 组柜优化 |
| `/gantt` | 一级 | 入炉计划 |
| `/release` | 一级 | 结果发布；`?tab=issues` 打开校验段 |
| `/pool` | **redirect** | `/demand` |
| `/workbench` | **redirect** | `/release` |
| `/validation` | **redirect** | `/release?tab=issues` |
| `/cabinets` | **redirect** | `/master/cabinets` |
| `/processes` | **redirect** | `/master/processes` |
| `/boxspecs` | **redirect** | `/master/boxspecs` |

登记说明：以上为 **实际 router 清单**。不存在独立 `/grouping`、`/validation` 一级组件路由（`ValidationCenterView.vue` 仅作 `/release` 子视图）。本 VM 未做浏览器书签刷新；jsdom `router.push` 旧 path 均落到新 path（C5-04 绿）。

---

## 6. 柜8 不当失败

| 项 | 记录 |
|----|------|
| 种子 | `seed-demo-plan.ts`：柜8 + `P013`（手术衣，`allowed: ['柜8']`），**本身合法**，`date/shift=null` 未排 Content |
| 故意冲突 | D002 行（如 `P001`，允许柜 9/20）写入柜8 → `CABINET_MISMATCH` error；`commit-gate` **A-AUTO-01** 仍断言 auto 拒绝落盘。这是演示/闸门样例，不是导航或 Vue 迁移回归 |
| 本轮处理 | **不**因柜8 冲突判 C4/C5 失败。C5 导航/筛选/导出用例使用 jsdom 改写 `plan.furnaces[0..2]` 的 `date`，不依赖把 D002 装进柜8 |

---

## 7. 必改 / 风险

### 7.1 必改

**无。** 本轮 C4/C5 必验项在既定执行方式下均通过。

### 7.2 风险（不阻塞本报告结论）

| 级别 | 风险 | 说明 | 建议 |
|------|------|------|------|
| 环境 | C4-05 未实起 | 本 VM 无 Docker daemon；compose/nginx/Dockerfile 静态正确 | MateBook：`docker compose up -d --build` 后 curl `127.0.0.1:18080/`，并硬刷新 `/master` `/demand` `/pack` `/gantt` `/release` 及旧书签 |
| 视觉 | C4-02 未量尺 | 令牌 168/30/40/28 已绑定；无同分辨率前后截图、无首屏行数 | 组柜 + 入炉计划各一张 100% 缩放截图，DevTools 量 `.sidebar` / `td` / `.topbar` |
| 手工 | C4-03 未走 18080 主线六路径 | B1–B9 单测仍绿，不能替代浏览器主线 | 有 Docker 后再走提纲第 5 节铁律表 |
| 壳层残留 | `/release` 已排期列表仍是旧 `WorkbenchView` | 双模式、分配、建议拼炉、拆炉、运行校验、查看甘特仍在结果发布列表区。**不是**全局顶栏泄漏结果日/CSV | 若要信息架构彻底收敛，后续热修把工作台操作收回组柜/入炉，结果发布只留列表+校验+导出 |
| 导出拦截 | `exportCSV()` 仍先 `collectIssues()`（含未排期组柜码） | 默认 `blockOnError: false`，C5-03 仍绿；若将来打开拦截，未排期硬错误可能挡住「该结果日」导出 | 导出拦截应对齐 `collectReleaseIssues()` / 当日已排期 |
| 主数据 | 「客户规则」无独立子路由 | `/master` 仅柜/工艺/箱规三 Tab，与迁移前主数据页一致，非 C5 新缺口 | 不作为本变更失败 |
| 工程 | GitHub Checks 空；npm audit 2 moderate；Node 22.14 vs 包引擎 22.22 | 不作为功能失败 | CI 与引擎警告另跟 |

---

## 8. 给开发的联验回执

| 自检项 | 联验 |
|--------|------|
| Vue 3 + 关键 `.vue` + 无 `src/ui` 双入口 | 成立 |
| 令牌 168 / 30 / 40 / 28 | 成立（未实测像素） |
| B1–B9 单测仍绿 | 成立（188） |
| `npm test` + `npm run build` | 绿，188 |
| compose `18080:80` + SPA fallback 文件 | 成立；未实起 |
| 侧栏仅五项且顺序正确 | 成立 |
| 全局顶栏无结果日 / 导出 CSV | 成立 |
| 结果日/导出/校验在 `/release`；组柜页内摘要 | 成立 |
| 组柜/甘特/策略/双模式/热修码仍可达 | 成立 |
| 旧 path redirect 已登记 | 见 §5 |
| 柜8 不当失败 | 已标注 |
| 未改 domain 口径 / 端口约定 | 静态成立（`src/domain/**` 本联验未改；compose 仍 18080） |

---

## 9. 测试结论记录

| 项 | 记录 |
|----|------|
| 测试环境/提交号 | Node 22.14.0 · 被测 `97e0ec37f4b49d3fc39f0382a06d22d7f84c887d` |
| 执行人/日期 | 测试侧联验执行者 · 2026-09-17 |
| C4-01～C4-05 | 通过 / 令牌通过 / 单测通过 / 188+build 绿 / 静态通过 |
| C5-01～C5-05 | 全部通过 |
| B1～B9 | 单测/静态仍成立；未做 18080 手工 |
| 阻塞问题 | 无 |
| 风险项 | 无 Docker 实起；无密度截图；`/release` 仍嵌旧工作台操作条 |
| 总结论 | **带风险通过** |
