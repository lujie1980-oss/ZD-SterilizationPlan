# 变更-4 · 中度紧凑密度对照（C4-02）

对照地址：主应用 `http://127.0.0.1:18080/`（实现结论） vs 原型 `http://127.0.0.1:8088/`（仅视觉参考，不作为实现结论）。

## 令牌真源

`src/app/styles/tokens.css` 镜像原型 `:root`：

| 项 | 令牌 | 目标 |
|----|------|------|
| 侧栏 | `--sidebar-w` | **168px**（折叠除外，约 160–180px） |
| 表体行高 | `--row-h` | **30px**（28–32px） |
| 表头/卡片头 | `--header-h` | **34px** |
| 顶栏 | `--topbar-h` | **40px** |
| 控件 | `--control-h` | **28px** |
| 甘特条 | `--gantt-bar-h` | **18px** |

## 手工测量（同分辨率、同数据、浏览器缩放 100%）

1. 打开组柜 `/pack` 与进炉计划 `/gantt`，DevTools 量 `.sidebar` 计算宽度、`.data td` 行高、`.topbar` 高度。
2. 记录首屏可见行数/柜卡数；相对迁移前（侧栏 220px、顶栏 56px、按钮 32px）应更紧凑且仍可扫读。
3. 错误/警告 Tag 颜色语义不变，仅内边距收敛。
4. 截图文件建议含：页面名、视口尺寸、日期、「迁移前/迁移后」。

柜8 演示冲突属故意数据，不当失败。
