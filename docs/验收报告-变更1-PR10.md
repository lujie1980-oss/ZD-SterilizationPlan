# 振德医疗 · 灭菌排产 · 变更-1 · 正式验收报告（PR #10）

| 项 | 内容 |
|----|------|
| 验收对象 | [PR #10](https://github.com/lujie1980-oss/ZD-SterilizationPlan/pull/10) · 分支 `cursor/change1-date-decouple-4cf9` |
| 被测提交 | `2d93f71beb9a6eaf6f6921d256fb62757b5edddf`（`test: add grouping page jsdom smoke for dual-entry UI`，其上为 `f9ee7e3` 功能提交） |
| 相对 GitHub `main`（`89c70be`） | `f9ee7e3`…`2d93f71` · **+2572 / −112**（27 files，PR 页口径） |
| 计划依据 | 《变更-1 测试提纲》v0.9；《领域模型》正式版 v1.1（Task **甘特才建**） |
| 执行日 | 2026-09-16 |
| 执行方式 | `npm install` → `npm test` → `npm run build` + 领域单测映射 + 源码静态核对 + **仓库外** `vite-node` 探针（C1-06/07/12/15/24/31/34/40）；**未做浏览器手工点击** |
| 验收角色 | 验收执行助手（**不改业务功能代码**；本文件为唯一新增交付） |

---

## 1. 总结论

**带风险通过。**

铁律四条在领域层与组柜 UI 接线均成立，提纲点名必核的 P0（C1-01/11、18–21、22/23、27、30/33/34、37/38、42/43、迭代 A 闸门）**全部通过**：

1. **组柜无 date/shift/Task**：`autoPackCabinet` / `manualPackCabinet` 写 `date=null`、`shift=null`、`taskId=null`；组柜页隐藏顶部结果日、无白夜班筛选。
2. **甘特才建 Task**：`applyGanttSchedule` 才写回 `date/shift/seq`、建 `CabinetTask` 链（prev/next / `isFirst` / `FurnaceSchedule.firstTaskId`）。
3. **入口 B 完整可组柜**：单击需求立刻 `listCandidateCabinets`（允许柜 ∩ 运行态）；灭菌中禁用可见。
4. **点行不勾选**：`focusDemand` 只改 `focusedId`；`toggleDemandCheck` 才进 `checkedIds`。jsdom 冒烟：点 `P001` 行后 checkbox 仍未选。

`npm test`：**124 / 124 通过**。`npm run build`：退出码 0。

**风险不破铁律，但挡住「全部 P0 全绿」**：C1-28 超托盘提示未接线；C1-31 手工再拼装填完毕柜无专用强预警；C1-40 `remainingBoxes` 写反且 `QTY_EXCEEDED` 未进入闸门。详见 §6 / §7。

---

## 2. 环境与命令结果

| 项 | 结果 |
|----|------|
| 工作目录 | `/workspace` |
| HEAD（被测） | `2d93f71beb9a6eaf6f6921d256fb62757b5edddf` |
| Node | v22.14.0 |
| npm | 10.9.7 |
| OS | Linux 6.12.94+ |
| 命令顺序 | `npm install` → `npm test` → `npm run build` |
| GitHub Checks | 该分支当时 **无 checks 输出**（不作为本轮功能失败） |

### 2.1 `npm install`

- **退出码**：0
- 新增 91 packages，审计 92 packages（约 2s）
- 弃用警告：`whatwg-encoding@3.1.1`（jsdom 传递依赖）
- npm audit：2 moderate；**不作为功能验收失败**

### 2.2 `npm test`（`vitest run` v3.2.7）

- **退出码**：0
- **Test Files 13 passed / Tests 124 passed (124)**，Duration ~2.21s

| 文件 | 用例数 | 本轮相关性 |
|------|--------|------------|
| `src/domain/__tests__/change1-grouping.test.ts` | 20 | C1-08/11/14/16/18–23/29/30/32/33/36–39/42/43 及 loadComplete 自动禁再拼 |
| `src/domain/__tests__/change1-ui.test.ts` | 2 | C1-11/19/22 工具条契约 + 三态芯片文案 |
| `src/domain/__tests__/change1-boot.test.ts` | 1 | jsdom：双入口、无班次筛选、灭菌中禁用可见、点行不勾选 |
| `src/domain/__tests__/export-csv.test.ts` | 2 | C1-02 未排炉次不进日计划 CSV |
| `src/domain/__tests__/commit-gate.test.ts` | 15 | 迭代 A 闸门 / C1-09 / C1-16 / C1-REG |
| `src/domain/__tests__/facts.test.ts` | 9 | C1-REG-A1 交期/指定柜芯片；C1-04 due 只读事实 |
| `src/domain/__tests__/plan-store.test.ts` | 12 | C1-REG-A2 `scheduleMode` 持久化 |
| `src/domain/__tests__/rule-engine.test.ts` | 21 | BOX_LIMIT 口径2、错柜 error |
| `src/domain/__tests__/excel-r1-r5.test.ts` | 14 | C1-REG-01 口径2 |
| `src/domain/__tests__/min-load.test.ts` | 10 | D002 阈值未回退 |
| `src/domain/__tests__/split-wizard.test.ts` | 5 | virtualLines 拆炉 |
| `src/domain/__tests__/suggest-combine.test.ts` | 4 | C1-10 建议拼炉基线（探针补断言 date/shift/task 为空） |
| `src/domain/__tests__/entry-scheduler.test.ts` | 9 | 进炉排序 / 重叠码；C1-06/07 同源 |

### 2.3 `npm run build`（`tsc --noEmit && vite build`）

- **退出码**：0
- Vite v6.4.3，29 modules transformed，built in 290ms
- 产物：`dist/index.html` 17.25 kB gzip 4.65 kB；`dist/assets/index-dG51JyXF.css` 20.59 kB gzip 4.70 kB；`dist/assets/index-CwcNFnza.js` 98.64 kB gzip 30.22 kB

---

## 3. 铁律核对（硬失败条件）

提纲与任务成功标准：**组柜无 date/shift/Task；甘特才建 Task；入口 B 完整可组柜；点行不勾选。** 另须迭代 A 双模式闸门不回退。

| 铁律 | 结论 | 量化 / 源码证据 |
|------|------|-----------------|
| 组柜无 date/shift | **成立** | `grouping.ts` `autoPackCabinet`/`manualPackCabinet` 字面量 `date: null, shift: null`。单测 C1-11/22：`packed.content.date/shift === null`。组柜页 `navigate('grouping')` 隐藏 `#topResultDate`；工具条默认调用 `groupingToolbarContractHtml()` **不**注入 `grpShiftTabs` / `grpOnlineDate`。jsdom：`#grpShiftTabs` 为 null |
| 组柜无 CabinetTask | **成立** | 组柜路径 `taskId: null`。C1-37：`trays.length>0` 且 `every(trayId)` 命中柜9 托盘主数据，`taskId===null`。`cabinet-task.ts` 仅被 `syncFurnacePlan` → `applyGanttSchedule` 调用 |
| 甘特才写回 date/shift 并建链 | **成立** | C1-23/38：写回后 `date='2026-07-24'`、`shift='白班'`、`seq=1`、`taskId` 有值、`isFirst===true`、`previousTaskId===null`、`schedules[0].firstTaskId===task.id`。auto 重叠则 **整次 abort**、`tasks=[]`（C1-06 探针） |
| 入口 B 完整可组柜列表 | **成立** | C1-33：P001 → `['柜20','柜9']`（长度 2>0）。C1-30：P012 → 柜14 `runtime=sterilizing`、`selectable=false`、文案匹配「灭菌中」。UI 标题「可组柜（允许设备 ∩ 运行态）」 |
| 点行不勾选 / checkbox 才多选 | **成立** | `focusDemand` 拷贝 `checkedIds` 不追加。C1-42/43 单测：focus P001 → `checkedIds=[]`；toggle P002 → 仅 P002。jsdom：点 `[data-grp-demand-view=P001]` 后 checkbox `checked===false`；click 处理器要求 `!t.closest('[data-grp-check]')` |
| 迭代 A auto 拒绝 error | **成立** | C1-16 / A-AUTO-01：P001→柜8 `aborted===true`、`persisted.lines===[]`。组柜自动路径 `decideCommit(..., editSource:'auto')` |
| 迭代 A manual 强预警 | **成立** | 组柜手动路径 `editSource:'manual'`；`needsOverridePrompt` 时炉卡/弹窗「手工违例」。commit-gate 15 例全过 |

过渡页「已排期浏览」的 `addFurnace` 仍写入 `state.date/shift`（一期日格子残留）。**不是组柜主路径**；提纲风险表已预告。不记铁律失败。

---

## 4. P0 逐条

覆盖标记：

- **已自动化覆盖**：Vitest 直接证明预期
- **探针核对**：仓库外 `vite-node` 一次性脚本（不入库）
- **仅静态核对**：对照 `app.ts` / `index.html` / 领域模块，无浏览器 E2E
- **未覆盖需手工**：需真实点击/刷新；不阻断本轮（任务允许单测+静态）

### 4.1 解耦与甘特写回（C1-01～10）

| 用例ID | 结果 | 覆盖 | 证据 | 备注 |
|--------|------|------|------|------|
| **C1-01** | **通过** | 已自动化 + 静态 | 组柜分配不读顶部日期；结果 `date=null`、`scheduleStatus='unscheduled'`。炉卡/分层摘要 `未排` 芯片（`!content?.date`） | 组柜页隐藏结果日控件 |
| **C1-02** | **通过** | 已自动化 | `export-csv.test.ts`：`date/shift=null` 的 F1 不进 CSV，仅 F2（已排）`rowCount===1`。过滤器 `f.date === opts.date && f.shift === opts.shift` | 默认日计划不含未排 |
| **C1-03** | **通过** | 已自动化 + 静态 | `applyGanttSchedule` 给未排写 `date/shift`、`status/scheduleStatus='scheduled'`、`seq`。UI「同步装炉结果」走该函数 | 与 C1-23/38 同路径 |
| **C1-04** | **通过** | 已自动化 | C1-23/38：写回前后 `poolById('P001').due` 同一引用未改。`deriveFacts` 只读 due，组柜/甘特不写 StockLine.due | 交期芯片与上线日分离 |
| **C1-05** | **通过** | 仅静态核对 | `#topResultDate` / `#wbDate` 只改 `state.date` 再 `currentFurnaces(date,shift)` 滤**已排期**；组柜候选 `listEligibleForCabinet` **无 date 参数**。不会 `furnaces=[]`、不改 due | 组柜页该控件隐藏 |
| **C1-06** | **通过** | 探针核对 | 同柜已排白班 + 未排 → auto：`aborted===true`、未排 `date` 仍 null、`tasks.length===0`、issue=`STERILIZE_OVERLAP` | **无入库专测**；逻辑在 `cabinet-task.ts` 155–164 行 |
| **C1-07** | **通过** | 探针核对 | 同上 fixture、`scheduleMode:'manual'`：`ok===true`、未排写回 `2026-07-24`/`夜班`、issues 含重叠、文案「手工预警」 | 无入库专测 |
| **C1-08** | **通过** | 已自动化 | 旧字面量带 date → `normalizeCabinetContent` 后 `scheduleStatus==='scheduled'` 且 `status==='scheduled'` | 符合「旧 plan 不低于一期」 |
| **C1-09** | **通过** | 已自动化 | 与 A-AUTO-01 / C1-16 同：auto 错柜 `CABINET_MISMATCH` error 不落盘；与是否未排无关（闸门看 `validateFurnace`） | 铁律未破 |
| **C1-10** | **通过** | 已自动化 + 探针 | `autoPack` 可产出未排；error 经 `decideCommit(auto)` 回滚。`suggestCombineD002Cab9` 新建炉 `date/shift/taskId` 皆空；`commitSuggestCombine` 仍 `editSource:'auto'` | 建议拼炉仍走零 error 闸门 |

### 4.2 组柜筛选与双入口（C1-11～25）

| 用例ID | 结果 | 覆盖 | 证据 | 备注 |
|--------|------|------|------|------|
| **C1-11** | **通过** | 已自动化 + jsdom | 工具条契约无上线日/入炉时间；jsdom `#grpShiftTabs` 不存在；组柜页隐藏 `#topResultDate` | 必核 |
| **C1-12** | **通过** | 已自动化 + 探针 | 柜9 完整列表 = `allowed.includes('柜9')` 且非限制；**不含** P004/P005/P015。探针：illegal 行 0 条。行集 `P001,P002,P003,P014` | 与 C1-18 同源 |
| **C1-13** | **通过** | 已自动化 + 静态 | auto 组柜 `date=null`；`grpLayerHint` 后渲染「① 分层 → ② 装柜」；每层展示 `trayId` | 首版非动画分步，提纲风险表允许 |
| **C1-14** | **通过** | 已自动化 | P001∩P014 → `['柜20','柜9']`，不含报废柜1 | 多行交集 |
| **C1-15** | **通过** | 探针 + 静态 | 入口 B 列表仅允许柜；P001 不含柜8/柜1。`runManualPack` 若绕过非法柜则 `decideCommit(manual)` 强预警（错柜 error） | 列表本身不可选非法柜 |
| **C1-16** | **通过** | 已自动化 | auto 错柜 `aborted`、`persisted[0].lines===[]`。UI `runAutoPack` abort 则 return、不 `persist` | 与迭代 A 一致 |
| **C1-18** | **通过** | 已自动化 | `rows.map(lineId)` 与 `hardEligible` 期望集 **sort 后全等**；P015/P004 不出现 | 必核；完整列表不得漏行 |
| **C1-19** | **通过** | 已自动化 + jsdom | 三态枚举钉死；芯片文案「已进本柜 / 已进其他柜 / 未排」。jsdom 点柜9 出现 `data-placement=inThisCabinet` | 必核 |
| **C1-20** | **通过** | 已自动化 + 静态 | 已组入 P001/P002/P014 仍可见；`inThis` 体积与 `trays.reduce(vol)` 同源；`fillRate>0`。空候选文案：「还可排入为空，柜并非空闲（见已进本柜）」；柜卡仍显示装柜率 | 必核；候选 0 ≠ 柜空 |
| **C1-21** | **通过** | 已自动化 | P003 已在柜20 时 auto 柜9：`skippedOther` 含 P003，`lines` 不含 | `skipInOtherCabinet` 默认 true |
| **C1-22** | **通过** | 已自动化 + jsdom | 无白夜班切换；未排 `shift=null`；候选函数无 shift 参数 | 必核 |
| **C1-23** | **通过** | 已自动化 | 甘特写回才有 shift；组柜前 `beforeShift===null` | 必核 |
| **C1-24** | **通过** | 探针 + 静态 | `manualPackCabinet` → date/shift/task 空、托盘 `cab-9-tray-01`。UI `editSource:'manual'` 走闸门：auto 拒 error / manual 强预警 | 无拖拽调层（首版一层一行） |
| **C1-25** | **通过** | 静态 + 同源单测 | 入口 B 自动：`autoPackBlocked` 过滤后取首个可组柜；`selectedLineIds` 限制候选；结果未排、无班次；`grpLayerHint=true` | 无独立「推荐柜」单测，路径闭合 |

### 4.3 托盘分层与运行态（C1-27～36）

| 用例ID | 结果 | 覆盖 | 证据 | 备注 |
|--------|------|------|------|------|
| **C1-27** | **通过** | 已自动化 + 静态 | `trays.every(t => t.trayId)` 且命中 `traysForCabinet('柜9')` 主数据。UI 层头展示 `displayName` + `trayId`。无 trayId 则 `hydrateTraysFromLines` 重绑 | 必核；禁止临时画条 |
| **C1-28** | **未通过** | 静态 + 探针 | `ISSUE_CODES.TRAY_REQUIRED` **仅常量**；`rule-engine.ts` / UI **零引用**「超托盘」。`hydrateTraysFromLines` 不比 Tray.`ratedLoadM3`/`maxBoxes` | **P0 缺口**，见 §6 G1 |
| **C1-29** | **通过** | 已自动化 + 静态 | P001：`salesOrderNo` 有值、`dimL/W/H>0`、`vol===boxes*boxVol`。组柜行渲染 `SO … · L×W×H` | 种子缺 dim 时用立方根占位 |
| **C1-30** | **通过** | 已自动化 + jsdom | 柜14 灭菌中仍在列表、`selectable=false`；jsdom 卡 `disabled` 或文案「灭菌中」。点击仍可查看并 toast「可查看但不可组柜」；auto/manual pack 均拒绝 sterilizing | 必核；不得隐藏 |
| **C1-31** | **部分通过** | 已自动化 + 探针 | **自动禁再拼**：`autoPackCabinet` 对 `loadComplete` `ok===false`、文案「装填完毕」。运行态派生 `loadComplete`。入口 B 橙标「待入炉 · 不当空闲」、`autoPackBlocked=true`。**手工再拼**：`manualPackCabinet` 在 loadComplete 柜上 `ok===true` 且 **issues 为空**（探针 C1-31-manual-repack **FAIL**） | 缺「手工再拼须强预警」 |
| **C1-32** | **通过** | 已自动化 | auto + error → `markLoadComplete.ok===false`；manual → `ok===true` 且 `manualViolation===true` | 装填完毕闸门 |
| **C1-33** | **通过** | 已自动化 + jsdom | 选 P001 立刻非空可组柜列表；演示允许柜集非空 | 必核 |
| **C1-34** | **通过** | 探针 + 静态 | 柜9 loadComplete：`selectable=true`（可见）+ `autoPackBlocked=true`；柜20 idle：可自动组；柜14 sterilizing：禁用可见。入口 B 自动 `filter(!autoPackBlocked)` 故待入炉不按空闲乱拼 | 必核；列表未按 idle 优先排序，靠过滤实现 |
| **C1-36** | **通过** | 已自动化 | `FurnaceRun` 类型别名可读；`trays[0].onTray[0].stockLineId==='P001'`。plan 双写 `contents`↔`furnaces` | 旧名仅别名 |

### 4.4 CabinetTask 与交互（C1-37～43）

| 用例ID | 结果 | 覆盖 | 证据 | 备注 |
|--------|------|------|------|------|
| **C1-37** | **通过** | 已自动化 | Content + Trays + OnTray；无 Task；date/shift 空 | 必核 |
| **C1-38** | **通过** | 已自动化 + 静态 | 甘特首次排入才建链并写回。`persist()` 写入 `tasks`/`schedules`/`nextTaskSeq` | 必核；Content↔Task 排期后 1:1 |
| **C1-39** | **通过** | 已自动化 | 同柜两份 active：`replaceCabinetActive` `ok===false`、`ACTIVE_CONTENT` | 一柜单一活动计划 |
| **C1-40** | **部分通过** | 探针 + 静态 | 首版整行上托盘，`autoPack`/`manualPack` 用 `findPlacement` **跳过已进其他柜**，避免同一 `lineId` 双占。但 `remainingBoxes` 过滤条件写反：P001 已占 280 箱时探针得 `occupied=280`、`remaining=280`（应为 0）。`QTY_EXCEEDED` 仅常量，引擎未发出 | **P0 缺口**，见 §6 G3 |
| **C1-42** | **通过** | 已自动化 + jsdom | 单击行只聚焦；checkbox 保持未选；文案匹配「可组柜」 | 必核 |
| **C1-43** | **通过** | 已自动化 + 静态 | 仅 `change` + `[data-grp-check]` 调用 `toggleDemandCheck`。入口 B 自动组柜：无勾选则 toast「请勾选需求后再自动组柜（单击行仅查看）」；有勾选才批量 | 必核 |

### 4.5 迭代 A 回归抽查

| 用例ID | 结果 | 覆盖 | 证据 | 备注 |
|--------|------|------|------|------|
| **C1-REG-A1** | **通过** | 已自动化 + 静态 | `facts.test.ts` A-FACT-01/02：交期/指定柜芯片仍在。组柜需求行与待灭菌池、工作台可排池均 `factStripHtml` | 组柜行亦带事实条 |
| **C1-REG-A2** | **通过** | 已自动化 + 静态 | A-MODE-01：`scheduleMode='manual'` + `overrideNotes` round-trip。组柜工具条 `#grpScheduleModeTabs` 绑定同一 `setScheduleMode` | 刷新肉眼确认仍 **未覆盖需手工** |
| C1-REG-01（P1） | 抽查通过 | 已自动化 | excel R1–R5 + BOX_LIMIT 口径2 未改写 | 非本轮必过 |
| C1-REG-02（P1） | 抽查通过 | 已自动化 | virtualLines / 拆炉 round-trip 仍在 | 非本轮必过 |

---

## 5. P0 总表（成功标准）

| 用例ID | 优先级 | 结论 |
|--------|--------|------|
| C1-01 | P0 | **通过** |
| C1-02 | P0 | **通过** |
| C1-03 | P0 | **通过** |
| C1-04 | P0 | **通过** |
| C1-05 | P0 | **通过** |
| C1-06 | P0 | **通过** |
| C1-07 | P0 | **通过** |
| C1-08 | P0 | **通过** |
| C1-09 | P0 | **通过** |
| C1-10 | P0 | **通过** |
| C1-11 | P0 | **通过** |
| C1-12 | P0 | **通过** |
| C1-13 | P0 | **通过** |
| C1-14 | P0 | **通过** |
| C1-15 | P0 | **通过** |
| C1-16 | P0 | **通过** |
| C1-18 | P0 | **通过** |
| C1-19 | P0 | **通过** |
| C1-20 | P0 | **通过** |
| C1-21 | P0 | **通过** |
| C1-22 | P0 | **通过** |
| C1-23 | P0 | **通过** |
| C1-24 | P0 | **通过** |
| C1-25 | P0 | **通过** |
| C1-27 | P0 | **通过** |
| C1-28 | P0 | **未通过** |
| C1-29 | P0 | **通过** |
| C1-30 | P0 | **通过** |
| C1-31 | P0 | **部分通过** |
| C1-32 | P0 | **通过** |
| C1-33 | P0 | **通过** |
| C1-34 | P0 | **通过** |
| C1-36 | P0 | **通过** |
| C1-37 | P0 | **通过** |
| C1-38 | P0 | **通过** |
| C1-39 | P0 | **通过** |
| C1-40 | P0 | **部分通过** |
| C1-42 | P0 | **通过** |
| C1-43 | P0 | **通过** |
| C1-REG-A1 | P0 | **通过** |
| C1-REG-A2 | P0 | **通过** |

**P0：36 通过 / 2 部分通过 / 1 未通过 / 共 39。铁律未破。**

点名必核子集（C1-01/11、18–21、22/23、27、30/33/34、37/38、42/43、REG-A）：**14 / 14 通过**。

---

## 6. 缺口与残留风险

| ID | 级别 | 说明 | 是否破铁律 |
|----|------|------|------------|
| G1 | **P0 缺口** | **C1-28**：超托盘容量无硬/软提示。`TRAY_REQUIRED` 未进入 `validateFurnace`；分层不读 Tray 额定 | 否（层仍绑主数据 trayId） |
| G2 | **P0 缺口** | **C1-31**：装填完毕后 **手工** `manualPackCabinet` 可再拼且 **无** 预警码/文案。自动路径已禁 | 否（自动不乱拼） |
| G3 | **P0 缺口** | **C1-40**：`remainingBoxes` 把「已含该行的 Content」滤掉再统计，导致已占用时剩余量仍为全量（探针：280/280）。`QTY_EXCEEDED` 未发出。整行去重仍在 | 否（当前无拆分量双占主路径） |
| G4 | 低 | jsdom C1-18 断言 `querySelector(unassigned) \|\| document.body.textContent` **恒真**，完整列表漏行不能靠该冒烟发现；领域 C1-18 已钉死 | 否 |
| G5 | 低 | 入口 A 柜卡对 loadComplete 只显示「装填完毕」，「待入炉 · 不当空闲」主要在入口 B | 否 |
| G6 | 信息 | 分层首版一层一行（超额行堆到最后托盘）；无拖拽调层。提纲风险：「不锁最优装柜率」 | 否 |
| G7 | 信息 | 过渡「已排期浏览」`addFurnace` 仍写 `state.date/shift`；日格子分配未完全下线 | 否（主路径已迁组柜） |
| G8 | 信息 | 本轮 **无浏览器 E2E**：F5 后组柜、甘特色条、点 checkbox 批量组柜的像素/toast 未点 | 否 |
| G9 | 信息 | C1-06/07 无入库单测，本轮用探针补齐；建议合入 PR #10 测试套件 | 否 |
| G10 | 低 | `occupiedBoxesOf` 对无 `trays` 的扁平行 `n += 0`（注释写「视为全占」但未加 boxes） | 否（组柜路径会 hydrate trays） |

P1 抽样（非本轮必过）：

| 用例 | 结论 |
|------|------|
| C1-17 layers/fillRate 可刷新还原 | **静态通过**：`persist` 双写 contents；`normalizeCabinetContent` 还原 trays/fillRate |
| C1-26 组柜筛选无班次/入炉日 | **静态通过**：组柜工具条无该类条件；工作台筛选不作用于组柜候选 |
| C1-35 idle 双入口可组 | **静态通过**：idle `selectable && !autoPackBlocked` |
| C1-41 甘特重排链一致 | **未专测**；`buildTaskChain` 按 date/shift 重链。UAT 补 |

---

## 7. 给开发的必改项 vs 可后续项

### 7.1 必改项（若要以「P0 全绿」封口；**不破铁律**）

本报告 **不得也不曾** 改业务功能代码。下列留给 PR #10 后续修订或 follow-up：

1. **C1-28**：装入超过 Tray `ratedLoadM3`/`maxBoxes` 时发出「超托盘」（或等价）提示；级别按规则表。装柜率分母仍为柜 `ratedLoadM3`（已成立）。
2. **C1-31**：`manualPackCabinet`（及 UI）在 `runtime===loadComplete` 时 **必须** 强预警（manual 可继续；auto 已禁）。不得无声再拼。
3. **C1-40**：修正 `remainingBoxes` 过滤方向；`Σ OnTray.boxes` 超 StockLine 剩余时按 mode 拒绝/强预警并真正发出 `QTY_EXCEEDED`。扁平行占用不要 `+= 0`。

### 7.2 建议（非阻塞）

1. 把 C1-06/07/31/40 探针升为入库单测；收紧 jsdom C1-18（不要 `|| textContent` 短路）。
2. UAT 15 分钟：组柜无日期班次 → 选柜完整三态 → 点行不勾选 → 勾选批量 → 同步甘特才出 date/shift/Task → auto 错柜不落盘。
3. 过渡工作台「添加炉次」改为未排（date/shift null），避免日格子残留误导。
4. 入口 A 柜卡与入口 B 统一「待入炉 · 不当空闲」文案。

### 7.3 明确不在本轮（提纲已声明）

最优装柜率、拖拽调层、浏览器 E2E 像素。C1-17/26/35/41 为 P1。

---

## 8. 对照领域模型 v1.1（静态）

| 模型点 | 实现 | 结论 |
|--------|------|------|
| 组柜 Content + Trays + OnTray；无 Task | `autoPack`/`manualPack` | 成立 |
| date/shift 组柜 null；甘特写回 | `applyGanttSchedule` | 成立 |
| Content↔Task 0..1；甘特后 1:1 | `taskId` null → 写回 | 成立 |
| FirstTask 在链容器；节点 prev/next/isFirst | `FurnaceSchedule.firstTaskId` + `CabinetTask` | 成立 |
| Tray 主数据；`trayId` 必填 | hydrate + C1-27 | 成立 |
| CabinetRuntime 独立；灭菌中禁用可见 | `cabinet-runtime.ts` + demo 柜14 | 成立 |
| 同柜单一活动 Content | `replaceCabinetActive` / `ACTIVE_CONTENT` | 成立 |
| StockLine 订单号/规格/体积 | 种子 + UI 行 | 成立 |
| OnTray 分量 ≤ 剩余 | 函数写反且未闸门 | **缺口 G3** |
| plan v2 双写 contents↔furnaces | `serializePlan` | 成立 |
| BOX_LIMIT 口径2 / minLoad / virtualLines | 相对迭代 A 未改口径 | 成立 |

---

## 9. 附录：组柜 / 甘特路径清单

| UI 动作 | date/shift/Task | 闸门 |
|---------|-----------------|------|
| 入口 A/B 自动组柜 | 保持 null / 无 Task | `editSource=auto`；sterilizing/loadComplete 域内先拒 |
| 入口 A/B 手动组柜 | 保持 null / 无 Task | `editSource=manual`；sterilizing 拒；**loadComplete 未预警（G2）** |
| 装填完毕 | 不写 date | auto 零 error；manual 可违例 |
| 进炉「同步装炉结果」 | **写回 date/shift/seq + 建 Task 链** | auto 重叠整次失败；manual 可预警保存 |
| 导出日计划 CSV | 只含匹配 date×shift 的已排 | 未排默认不含（C1-02） |
| 顶部/工作台结果日 | 只过滤已排期视图 | 不改组柜候选、不清空未排、不改 due |
| 建议拼炉（过渡页） | 新建炉未排 | 内部 auto，error 回滚 |
| 过渡页添加炉次 | **仍写当前结果日/班次** | 非组柜主路径（G7） |

---

*本报告仅新增于 `docs/验收报告-变更1-PR10.md`，未修改业务功能代码。被测提交保持 `2d93f71beb9a6eaf6f6921d256fb62757b5edddf`。*
